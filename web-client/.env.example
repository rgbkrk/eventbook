# EventBook Web Client Environment Configuration

# Server URL for the EventBook Rust server
# In development, the Vite proxy handles this automatically
# In production, set this to your deployed server URL
VITE_SERVER_URL=http://localhost:3000

# Optional: Enable debug logging in the browser console
VITE_DEBUG=false

# Optional: Event sync interval in milliseconds
VITE_SYNC_INTERVAL=30000

# Optional: Enable offline mode (future feature)
VITE_OFFLINE_MODE=false
